import { ControlkeyoffDirective } from './controlkeyoff.directive';

describe('ControlkeyoffDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlkeyoffDirective();
    expect(directive).toBeTruthy();
  });
});
