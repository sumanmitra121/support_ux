import { RightclickoffDirective } from './rightclickoff.directive';

describe('RightclickoffDirective', () => {
  it('should create an instance', () => {
    const directive = new RightclickoffDirective();
    expect(directive).toBeTruthy();
  });
});
